그래프 구성
	노드 입력
	대표노드가 없으면
		대표노드 입력
	있으면
		지금까지 넣어진 아이들과 서로 엣지를 만듬
	
	엣지 입력

다익스트라 (그래프, 처음 시작)
/* 다익스트라 (그래프, 환승정보, 처음 시작, 도착) */
	처음 시작 노드의 걸리는 시간 (0)
	처음의 이전의 노드 (무시)

	모든 그래프의 노드는
		처음 시작이 아니면
			거리 미정
			이전도 미정
		검사안된노드리스트에 넣음
		/** 프리오리티 큐에 넣음 (노드, 거리)로 */

	검사안된노드가 없을 때까지
		/* 이건 따로 구현 */
		u = 최소 거리가 걸리고 있는 노드
		/** 민힙에서 하나 빼오기 */
		/* 추가
		이게 도착이면
			끝
		*/
		검사안한노드에서 지워버린다
		/** 프리오리티큐에서는 자동으로 지워진다 */
		
		u에 인접한 노드들을 얻는다
		/*** 환승역이면 같은 게 여러 곳에 있는데 어떡하지 대표역? 대표역
				그냥 거리가 0인 지점으로 이어버림 */
			최소의 거리 = u의 거리 + u부터 인접한 노드까지의 거리
			최소의 거리가 인접한 노드의 거리보다 작으면
				대입
				인접한 노드의 이전은 u
				/** 프리오리티를 낮추기 흠... 이 부분은 모르겠다 */
				/** 삭제하고 다시 넣기 뿐인가 */

	걸리는 시간의 어레이와 이전의 노드들을 반환한다
/*
	S = []
	u는 타겟 이겠지 (?)
	u 이전이 안 정해져있을 때까지
		u를 S에 넣는다
		u는 u의 이전노드
	S 반환
*/
